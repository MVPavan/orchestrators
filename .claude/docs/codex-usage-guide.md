# Codex Plugin Usage Guide — LEGACY (broker-era)

> **Superseded — do not follow this.** This guide documented the old `openai-codex` Claude Code
> plugin and its `codex-companion.mjs` broker: jobs, `status`/`result`/`cancel`, the shared
> single-flight runtime, the stop-time review gate, and the `codex:codex-rescue` subagent. That
> plugin is no longer used in this repo.
>
> - **Current authority:** [`.claude/commands/use-codex.md`](../commands/use-codex.md).
> - **Current integration:** the `codex-adapter` plugin (install: marketplace `MVPavan/codex-adapter`).
>   Stateless `codex exec`, role-based, concurrent. See [`.claude/commands/use-codex.md`](../commands/use-codex.md).
>
> Kept only as a historical record of the broker-era design.
